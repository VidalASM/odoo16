odoo.define('odoope_ruc_validation_pos.model', function(require) {
    var models = require('point_of_sale.models');
    // models.load_fields('res.company', ['l10n_pe_ruc_validation','l10n_pe_dni_validation']);
});